name: baby_cry_interpreter_ai
description: AI-powered baby cry interpreter using Gemini 3 Flash.
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  record: ^5.0.0
  path_provider: ^2.1.0
  flutter_dotenv: ^5.1.0
  permission_handler: ^11.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
